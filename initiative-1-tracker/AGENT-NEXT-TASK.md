# Agent next task

**Task ID:** T1.1a

## Task row (acceptance criteria)

| ID | Sub-task | Acceptance criteria | Done |
|----|----------|---------------------|------|
| T1.1a | Create project folder (e.g. `tracker/` or under `initiative-1-tracker/`) and init (npm/pip) | `package.json` or `requirements.txt` exists; `npm install` / `pip install` runs | [ ] |

## Instructions for the agent

1. Implement the sub-task in the Tracker codebase; meet the acceptance criteria.
2. Run tests: `cd initiative-1-tracker/tracker && node test/run.js` (add tests if the task implies new behavior).
3. Mark the matching row in `initiative-1-tracker/TASKS.md` as `[x]` in the Done column.
4. Commit on branch `agent/T1.1a` and push; open a PR for human review.

_Generated by `node scripts/agent-get-next-task.js --write-file`. Re-run to refresh after completing a task._
